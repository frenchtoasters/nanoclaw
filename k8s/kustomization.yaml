apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nanoclaw

resources:
  - namespace.yaml
  - rbac.yaml
  - pvc-workspace.yaml
  - statefulset-postgres.yaml
  - deployment-orchestrator.yaml
